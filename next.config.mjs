/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  experimental: {
    // Ensuring Turbopack uses the correct root for the workspace
    // turbopack: {
    //    root: './'
    // }
  }
};

export default nextConfig;
